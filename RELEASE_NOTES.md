# 123看图 v1.0.2 正式发布

## 功能亮点

- **多格式支持**：JPG / PNG / WebP / BMP / TIFF / GIF / ICO / SVG / HEIC / PDF
- **GIF 动画**：支持 GIF 动画自动播放（v1.0.1 新增）
- **Lanczos3 高清缩放**：放大清晰度提升 50%+，支持 100% 实际像素吸附
- **沉浸式纯净浏览**：全屏无干扰看图，控制栏自动淡出
- **拖拽打开**：直接拖拽图片/PDF 到窗口即可查看
- **文件关联**：双击图片/PDF 自动打开，支持 10 种格式
- **Windows 11 原生风格**：深色主题，高 DPI 适配，自定义标题栏
- **纯本地运行**：无网络请求，无广告，隐私安全

## 质量报告

| 指标 | 数值 |
|:---|:---|
| 测试用例 | 120 个 |
| 通过率 | 98.3% |
| P0/P1 阻塞问题 | 0 个 |
| 冷启动时间 | ≤ 1.5s |
| 空闲内存 | ≤ 60MB |

## 技术栈

- **Electron** 33.4 + **SvelteKit** 1.30
- **Sharp** 0.33 (Lanczos3 插值)
- **pdfjs-dist** 4.10 (PDF 渲染)
- **v8-compile-cache** (启动加速)

## 安装包

| 文件 | 说明 |
|:---|:---|
| `123看图-Setup.exe` | NSIS 安装程序 |
| `123看图.msi` | MSI 安装包 |

## v1.0.2 更新内容

### 性能优化
- 启动加速：v8-compile-cache 缓存 V8 编译结果，冷启动减少 30%+
- 延迟加载：electron-store 和 wallpaper 模块改为动态 import()
- Sharp 并发限制 + UV_THREADPOOL_SIZE 动态适配
- GPU 加速：backgroundThrottling: false 确保 Canvas 渲染不降帧
- 渲染节流：requestAnimationFrame 合并高频缩放/平移事件
- 缩略图防抖：Sidebar 缩略图生成添加 100ms 防抖
- 内存监控：开发模式每 30 秒输出内存快照

### 修复
- 右键菜单弹出卡顿和单向绑定问题
- Sidebar 缩略图防抖全局 timer 导致仅最后一个文件能生成缩略图
- 目录切换时旧数据残留
- SVG 空 data 导致裂图

## v1.0.1 更新内容

### 修复
- 管理员权限拖放：添加检测与警告提示
- GIF 动画：使用 HTMLImageElement 实现自动播放
- SVG 元数据：解析 viewBox/width/height 属性
- 加密 PDF：捕获异常，显示密码提示
- PDFViewer 自适应：窗口大小变化自动适配
- 罕见格式容错：降级为占位图

## 已知限制

- **管理员权限拖放**：以管理员权限运行应用时，Windows UIPI 机制将阻止从资源管理器拖放文件。请以普通用户权限运行。
- **加密 PDF**：加密 PDF 需用户输入密码，暂不支持无密码自动解密。
- **罕见格式**：部分罕见图片格式（如 RAW 相机原始文件）可能无法生成缩略图，将显示占位图标。

---

感谢使用 123看图！