# 123看图 安装指南

## 系统要求

- **操作系统**：Windows 10 21H2 或更高版本 / Windows 11 22H2 或更高版本
- **架构**：64 位 (x64) 处理器
- **内存**：至少 512MB RAM
- **磁盘空间**：至少 150MB 可用空间

## 安装步骤

### 方式一：NSIS 安装程序（推荐）

1. 下载 `123看图-Setup.exe`
2. 双击运行安装程序
3. 按照向导提示完成安装
4. 可选择安装路径（默认：`C:\Users\<用户名>\AppData\Local\Programs\123看图`）
5. 可选择是否创建桌面快捷方式
6. 安装完成后，从桌面或开始菜单启动

### 方式二：MSI 安装包

1. 下载 `123看图.msi`
2. 双击运行安装包
3. 按照向导提示完成安装
4. 安装完成后，从桌面或开始菜单启动

## 快捷键

| 快捷键 | 功能 |
|:---|:---|
| `Ctrl+O` | 打开文件 |
| `Ctrl+=` / `Ctrl+-` | 放大 / 缩小 |
| `Ctrl+0` | 适应窗口 |
| `Ctrl+1` | 实际尺寸 (100%) |
| `Ctrl+R` / `Ctrl+L` | 右旋 / 左旋 90 度 |
| `Ctrl+H` / `Ctrl+V` | 水平 / 垂直翻转 |
| `F11` / `Ctrl+Shift+F` | 沉浸模式切换 |
| `←` / `→` | 上一张 / 下一张 |
| `ESC` | 退出沉浸模式 / 关闭菜单 |
| `Ctrl+C` | 复制图片到剪贴板 |
| `Ctrl+W` | 设置壁纸 |
| `Delete` | 删除图片 |

## 卸载

- 通过 Windows 控制面板 → 程序和功能 → 123看图 → 卸载
- 或从开始菜单右键 123看图 → 卸载

## 注意事项

- **建议以普通用户权限运行**（默认）。拖拽功能需要普通权限，以管理员身份运行会导致拖拽失效。
- 纯本地运行，无网络请求，无广告。
- 文件关联在安装时自动注册，卸载后自动恢复系统默认。

## 常见问题

**Q：拖拽文件无反应？**
A：请确认应用不是以管理员身份运行。在快捷方式属性中取消"以管理员身份运行此程序"选项。

**Q：壁纸设置失败？**
A：确认图片格式支持，部分系统壁纸设置需要管理员权限。

**Q：PDF 打开失败？**
A：检查 PDF 是否加密，加密 PDF 会提示输入密码。

**Q：GIF 不播放动画？**
A：GIF 动画已支持自动播放，如遇不播放请检查文件是否损坏。

**Q：SVG 显示不清晰？**
A：SVG 为矢量格式，缩放时自动重渲染，尺寸信息通过解析 viewBox/width/height 属性获取。